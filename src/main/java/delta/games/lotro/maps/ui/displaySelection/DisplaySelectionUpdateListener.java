package delta.games.lotro.maps.ui.displaySelection;

/**
 * Listener for display selection (item visibility) updates.
 * @author DAM
 */
public interface DisplaySelectionUpdateListener
{
  /**
   * Called when the display selection (item visibility) has been updated.
   */
  void displaySelectionUpdated();
}
